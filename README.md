# Second Army Checker (SAC)

Utility for tracking clan members activity.

## How to use
1. Put `SAC.exe` in a folder
2. Add `input.csv`
3. Run the program
4. Use commands:
   - N → create output.csv
   - C → check files
   - S → start processing
   - E → exit
